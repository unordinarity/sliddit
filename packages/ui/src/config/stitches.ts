import { createCss, IConfig } from '@stitches/react'

export const stitchesConfig: IConfig = {
  theme: {
    colors: {

    },
    fonts: {

    },
  },
  themeMap: {}
}

export const stitches = createCss(stitchesConfig)
